import 'package:flutter/material.dart';

class JsScreen extends StatelessWidget {
  const JsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
